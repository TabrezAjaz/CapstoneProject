# Python dependencies for the Container Image Vulnerability Scanner
#
# NOTE: Trivy itself is an external binary (installed separately — see the
# Sprint 1 report). These packages are only for the Python wrapper.

PyYAML>=6.0        # read config.yaml
